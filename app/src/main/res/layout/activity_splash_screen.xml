<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/splash_screen_bg_color"
    tools:context=".Activity.SplashScreenActivity">

    <ImageView
        android:id="@+id/splash_screen_act_logo_view"
        android:contentDescription="@string/splash_screen_act_logo_image_desc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:keepScreenOn="true"
        android:src="@drawable/splash_screen_act_logo"
        />

    <ImageView
        android:id="@+id/splash_screen_gadgets_logo_view"
        android:contentDescription="@string/splash_screen_act_logo_image_desc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:src="@drawable/splash_screen_gadgets_logo"
        />

</RelativeLayout>